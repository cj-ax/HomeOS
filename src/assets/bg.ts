// Background image — extracted from smart-home-dashboard.jsx
// This is a base64-encoded JPEG loaded from bg.txt at build time.
// For now, we import the raw string.
import bgData from './bg.txt?raw';
export const BG = bgData;
